// This file is ready for your custom JavaScript.

// The previous fade-in animation was removed because it is not
// well-suited for a horizontal scrolling container.

// You can add other interactive features here in the future.

document.addEventListener('DOMContentLoaded', () => {
    // This event listener ensures your code runs only after the
    // entire HTML document has been loaded and parsed.
    console.log("Website loaded and ready.");
});
